require "test_helper"

class BookingTest < ActiveSupport::TestCase
  # the booking model requires both passenger and flight
  # the booking model defaults to 1 for number of passenger
  # the associations are correct
end
